/**TODO
 * ->Add function for add, minus, multiply and division
 * ->function named operate that has parameter of firstNum,lastNum,operator that picks the operations functions
 * ->create a function to update display
 * ->store number in display when operator is click and the operator clicked
 * ->call operate on = pressed
 * ->Once the operate is called then display solution
 * **/

let firstNumber;
let operator;
let lastNumber;
let display;
